# Production environment sample.
# Keep real secret values outside git and inject them through the deployment platform.

APP_ENV=prod
APP_DEBUG=0

DATABASE_URL=
MESSENGER_TRANSPORT_DSN=doctrine://default?auto_setup=0

OPENAI_API_KEY=
OPENAI_PRODUCT_ENRICHMENT_MODEL=gpt-4o-mini
OPENAI_PRODUCT_MAX_ACTIVE_BATCHES=1

NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_USE_MOCKS=0
